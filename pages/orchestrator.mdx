import { Button, Container } from 'theme-ui'
import ForgeApi from '../components/forgeApi'

export default (props) => (
  <Container as='main' py={5} variant='copy' {...props} />
)

# Pangeo-Forge API

<ForgeApi></ForgeApi>
